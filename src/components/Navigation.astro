---
// const pathname = new URL(Astro.request.url).pathname;
// const currentPath = pathname.slice(1); // remove the first "/"

const pathname = new URL(Astro.request.url).pathname;
const originalString = pathname;
let currentPath = originalString.replace(/^\/|\/$/g, "");
// console.log(currentPath);
// console.log("pathname", pathname);
// console.log("currentPath", currentPath);
---

<!-- astro way it do not work when I deploy on netlify on localhost it is working-->
<div class="nav-links">
  <a class={currentPath === "" ? "active" : ""} href="/">Home</a>
  <a class={currentPath === "work" ? "active" : ""} href="work">Portfolio</a>
  <a class={currentPath === "about" ? "active" : ""} href="about">About</a>
</div>
